#include <stdio.h>
#include <string.h>
int main()
{
	char txt[50][50]={{" "}};
	int loop, num1=0, num2=0;
	scanf("%d", &loop);
	for (int i=0; i<loop; i++){
		printf("%d\n", i);
	}
	return 0;
}